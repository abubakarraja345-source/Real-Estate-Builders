import { PagePlaceholder } from "@/components/shared/page-placeholder";

export default function AdminPropertiesPage() {
  return (
    <PagePlaceholder
      title="Properties"
      description="Manage property listings once connected to Supabase."
    />
  );
}
