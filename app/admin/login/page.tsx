import { PagePlaceholder } from "@/components/shared/page-placeholder";

// TODO: replace with a real sign-in form once Supabase auth is configured.
export default function AdminLoginPage() {
  return (
    <PagePlaceholder
      title="Admin Login"
      description="Sign-in will be available once authentication is configured."
    />
  );
}
